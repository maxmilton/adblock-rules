{
  "servers": {
    "filesystem": {
      "command": "bunx",
      "args": ["@modelcontextprotocol/server-filesystem", "${workspaceFolder}"]
    },
    "playwright": {
      "command": "bunx",
      "args": ["@playwright/mcp", "--executable-path", "/usr/bin/brave", "--isolated"]
    }
  }
}
